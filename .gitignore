# Ignore everything in the tmux dir
/.tmux
# Except the tpm plugin
!/.tmux/plugins/tpm

# Ignore the bashrc files with private info
/.bashrc.d/*.private.bashrc

# Ignore company-specific Claude skills and commands
/.claude/skills.private
/.claude/commands.private

# Beads issue tracker
# Ignore SQLite database files (local cache)
/.beads/*.db
/.beads/*.db-*
/.beads/*.db-shm
/.beads/*.db-wal

# JSONL files: Ignore by default (only track in solo projects)
# Use `git add -f .beads/*.jsonl` to force-add in solo repos
/.beads/*.jsonl
